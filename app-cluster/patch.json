{
  "patch_policy": [""],
  "patch_nodes": [{
    "container": {
        "snapshot": "ss-facf5rbq",
        "zone": "pek3"
    },
    "patch": [{
      "mount_role": "master",
      "mount_point": "/upgrade",
      "mount_options": "defaults,noatime",
      "filesystem": "ext4",
      "cmd": "/upgrade/opt/app/bin/node/upgrade.sh"
    }],
    "rollback": [{
      "mount_role": "master",
      "mount_point": "/upgrade",
      "mount_options": "defaults,noatime",
      "filesystem": "ext4",
      "cmd": "echo"
    }]
  }]
}
