{
  "patch_policy": ["", "appp-d8da86od"],
  "patch_nodes": [{
    "container": {
        "snapshot": "ss-dukcuvin",
        "zone": "pek3"
    },
    "patch": [{
      "mount_role": "",
      "mount_point": "/upgrade",
      "mount_options": "defaults,noatime",
      "filesystem": "ext4",
      "cmd": "dpkg -i /upgrade/ifupdown_0.8.17ubuntu1.1_amd64.deb"
    }],
    "rollback": [{
      "mount_role": "",
      "mount_point": "/upgrade",
      "mount_options": "defaults,noatime",
      "filesystem": "ext4",
      "cmd": "echo"
    }]
  }]
}

