{
  "patch_policy": [""],
  "patch_nodes": [{
    "container": {
        "snapshot": "ss-a0qr7bdl",
        "zone": "pek3"
    },
    "patch": [{
      "mount_role": "node",
      "mount_point": "/upgrade",
      "mount_options": "defaults,noatime",
      "filesystem": "ext4",
      "cmd": "systemctl disable zabbix-agent2.service"
    }],
    "rollback": [{
      "mount_role": "node",
      "mount_point": "/upgrade",
      "mount_options": "defaults,noatime",
      "filesystem": "ext4",
      "cmd": "echo"
    }]
  }]
}
